\documentclass{article}
\usepackage{graphicx} % Required for inserting images

\title{Mathematical Equations}
\author{GAURANG TYAGI}
\date{19, February 2026}

\begin{document}

\maketitle

\section{Equations}
{\begin{equation}
    f(x) = f(a) + f'(a)(x-a) + \frac{f''(a)}{2!}(x-a)^2 + \frac{f'''(a)}{3!}(x-a)^3 + ... + \frac{f^{(n)}(a)}{n!}(x-a)^n
\end{equation}} 
\begin{equation}
    R_n = \frac{f^{n+1}(\epsilon)}{(n+1)!}(x-a)^{n+1}
\end{equation}
\begin{equation}
    f(x) = \frac{a_0}{2} + \sum_{i=1}^{\infty}[a_ncos(\frac{n\pi{x}}{L})+b_nsin(\frac{n\pi{x}}{L})]
\end{equation}
\begin{equation}
    \rho(\frac{\partial{v}}{\partial{t}} + (v.\nabla)v) = -\nabla{p} + \mu\nabla^2v + f 
\end{equation}
\begin{equation}
    det(A) = \sum_{\sigma\epsilon{S}_n}sgn(\sigma)\prod_{i=1}^na_{i,\sigma_i}\}
\end{equation}
\begin{equation}
    det(I + \lambda{K}) = \sum_{n=0}^\infty\frac{\lambda^n}{n!}\int...\int{det[K(x_i,x_j)]_{i,j=1}^n}dx...dx_n
\end{equation}
\begin{equation}
    f(x)=\begin{cases}
        x^2 + 1,\quad x>0, \\
        det(A - xI),\quad x<=0 \ and \ A \ \epsilon \ R^{n\times{n}} , \\
        \int_{0}^3 t^3dt,\quad x \ \epsilon \ R,
    \end{cases}
\end{equation}
\end{document}
