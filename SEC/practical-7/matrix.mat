first_row = 3*(1:6);
middle_rows = randi(10,3,6);
last_row = (1:6).^2;

mat = [first_row;middle_rows;last_row];
display(mat);

% dimensions
size(mat);
display(ans);

% number of elements

numel(mat);
display(ans);

% reshaping

reshape(mat,1,30);
display(ans);

reshape(mat,2,15);
display(ans);

reshape(mat,3,10);
display(ans);

reshape(mat,5,6);
display(ans);

reshape(mat,6,5);
display(ans);

reshape(mat,10,3);
display(ans);

reshape(mat,15,2);
display(ans);

reshape(mat,30,1);
display(ans);